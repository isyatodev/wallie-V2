python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
pyyaml>=6.0
loguru>=0.7.2

# LLM providers
openai>=1.40.0
anthropic>=0.34.0
google-generativeai>=0.7.0

# HTTP / WebSocket
aiohttp>=3.9.0
httpx>=0.27.0
websockets>=12.0

# Audio
sounddevice>=0.4.6
numpy>=1.26.0

# Vision
mss>=9.0.1
pillow>=10.2.0
imagehash>=4.3.1

# Chat platforms
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# Donations — Streamlabs Socket API (Socket.IO 2.x protocol; v5+ speaks 3.x and will NOT connect)
python-socketio>=4.7.0,<5

# Dashboard
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Optional: local zero-cost TTS (Piper). If you only use Fish/EL you can skip these.
# piper-tts>=1.2.0
# onnxruntime>=1.17.0

# Optional: local zero-cost TTS (Kokoro) — higher quality than Piper, no API key.
# Needs PyTorch. Install with: pip install kokoro soundfile
# kokoro>=0.9.2
# soundfile>=0.12.1

# Optional: HEARING (system-audio loopback + speech-to-text + music analysis).
# Required only if you enable hearing. Install with: pip install soundcard faster-whisper
# soundcard>=0.4.3
# faster-whisper>=1.0.0

# Utilities
orjson>=3.10.0
